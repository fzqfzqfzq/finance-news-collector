[railway]
  [railway.variables]
    NODE_ENV = "production"

[[railway.environment]]
  PORT = "3000"

[build]
  builder = "NIXPACKS"

[[deploy]]
  root = "backend"
  startCommand = "node server.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
